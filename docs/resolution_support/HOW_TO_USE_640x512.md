# 640×512 해상도 사용 가이드

## 🎯 빠른 요약

### 현재 코드 상태
- **지원 해상도**: 640×512, 640×384
- **저장 폴더**: 동적으로 `{width}x{height}_newest/` 생성
- **하드코딩**: ❌ 제거됨
- **확장성**: ✅ 새 해상도 추가 가능

### 640×512 실행 방법
```bash
python integrated_pcd_depth_pipeline_newest.py \
    --parent_folder D:\data\ncdb-cls\ncdb-cls\2025-07-11_15-00-27_410410_A\synced_data
```

**그뿐입니다!** 640×512와 640×384가 모두 자동으로 생성됩니다.

---

## 🔍 상세 로직 흐름

### 📊 1단계: 초기화 (라인 1058-1103)

#### A. 해상도 정의
```python
resized_resolutions = [(640, 512), (640, 384)]
```
- **역할**: 처리할 해상도 목록
- **특징**: 리스트 형태 → 추가하면 자동 지원
- **기존 방식**: `if process_resized:` (640×384 하드코딩)

#### B. 해상도별 폴더 생성
```python
for res_size in resized_resolutions:  # (640, 512), (640, 384) 순서대로 처리
    res_name = f"{res_size[0]}x{res_size[1]}"  # "640x512" 또는 "640x384"
    resized_base_dir = base_dir / f"{res_name}_newest"  # base_dir/640x512_newest
    process_resized[res_size] = not resized_base_dir.exists()  # 폴더 없으면 True
    
    if process_resized[res_size]:  # 새로 생성해야 하면
        # 이 해상도의 모든 폴더 경로 저장
        resized_dirs[res_size] = {
            'base': resized_base_dir,
            'pcd': resized_base_dir / "newest_pcd",
            'depth_maps': resized_base_dir / "newest_depth_maps",
            'viz_results': resized_base_dir / "newest_viz_results",
            'colormap': resized_base_dir / "newest_colormap",
            'synthetic_depth_maps': resized_base_dir / "newest_synthetic_depth_maps",
            'diff_results': resized_base_dir / "diff_results",
        }
        # 모든 폴더 생성
        for dir_path in resized_dirs[res_size].values():
            dir_path.mkdir(parents=True, exist_ok=True)
```

**생성 결과**:
```
synced_data/
├── 640x512_newest/              ← 새로 생성 (첫 실행)
│   ├── newest_pcd/
│   ├── newest_depth_maps/
│   ├── newest_viz_results/      ← RGB+Depth
│   ├── newest_colormap/
│   ├── newest_synthetic_depth_maps/
│   └── diff_results/
│
├── 640x384_newest/              ← 새로 생성 (첫 실행)
│   ├── newest_pcd/
│   ├── newest_depth_maps/
│   ├── newest_viz_results/      ← RGB+Depth
│   ├── newest_colormap/
│   ├── newest_synthetic_depth_maps/
│   └── diff_results/
│
└── (기존) 원본 해상도 폴더들...
```

#### C. 상태 추적
```python
# 딕셔너리로 각 해상도의 상태 추적
process_resized = {
    (640, 512): True,   # 첫 실행 시 True (폴더 없음)
    (640, 384): True,   # 첫 실행 시 True (폴더 없음)
}

resized_dirs = {
    (640, 512): {폴더경로 dict},
    (640, 384): {폴더경로 dict},
}
```

**장점**:
- ✅ 각 해상도 독립적 추적
- ✅ 새 해상도 추가 시 자동 적용
- ✅ 폴더 재생성 방지 (이미 있으면 skip)

---

### 📝 2단계: PCD 반복 처리 (라인 1130-1135)

```python
for pcd_file in pcd_files:  # 모든 PCD 파일 처리
    # ... 원본 해상도 (1920x1536) 처리 ...
    
    # 여기부터: 640×512, 640×384 처리
    for resized_image_size in resized_resolutions:
        # 각 PCD 파일마다 모든 해상도 처리
```

**처리 순서**:
```
PCD 파일 1 (0000000001.pcd)
├─ 1920×1536 처리
├─ 640×512 처리 ← 깊이맵, RGB+depth, colormap 생성
└─ 640×384 처리 ← 깊이맵, RGB+depth, colormap 생성

PCD 파일 2 (0000000002.pcd)
├─ 1920×1536 처리
├─ 640×512 처리 ← 깊이맵, RGB+depth, colormap 생성
└─ 640×384 처리 ← 깊이맵, RGB+depth, colormap 생성

...
```

---

### 🎨 3단계: 해상도별 처리 루프 (라인 1219-1298)

#### A. 스킵 체크
```python
for resized_image_size in resized_resolutions:
    res_name = f"{resized_image_size[0]}x{resized_image_size[1]}"
    
    # ⚠️ 중요: 이미 생성된 폴더는 건너뜀
    if not process_resized[resized_image_size]:
        continue  # (640, 512) 폴더가 이미 있으면 skip
```

**이렇게 하는 이유**:
- 재실행 시 중복 처리 방지
- 빠른 실행 속도 유지

#### B. 깊이맵 생성 (리사이징된 해상도)
```python
# 원본 (1920×1536) 깊이맵을 생성했으므로,
# 이제 리사이징된 해상도의 깊이맵 생성

depth_orig_resized, _ = projector.project_cloud_to_depth_map_with_labels(
    camera_name, orig_pts, lbl_orig, resized_image_size  # (640, 512) 또는 (640, 384)
)
```

**왜 별도로 생성?**
```
원본 (1920×1536)에서 보간법으로 640×512로 축소하면
➜ 정확도 손실

직접 카메라 프로젝션으로 640×512 생성하면
➜ 정확도 유지 ✓

즉, 각 해상도마다 독립적인 깊이맵 생성!
```

#### C. 경로 설정 (해상도별 동적 경로)
```python
dirs = resized_dirs[resized_image_size]  # 이 해상도의 폴더 dict 가져옴

# 이렇게 동적으로 경로 설정
resized_viz_path = dirs['viz_results'] / f"{stem}_depth_analysis.png"
#                  ↓                      ↓
#        640x512_newest/newest_viz_results/0000000001_depth_analysis.png
#        또는
#        640x384_newest/newest_viz_results/0000000001_depth_analysis.png
```

**이전 방식 (하드코딩)**:
```python
# 640x384만 고정
resized_viz_path = base_dir / "640x384_newest" / "newest_viz_results" / f"{stem}_depth_analysis.png"
# 640x512 추가하려면 추가 if 문 필요 ❌
```

#### D. RGB+Depth 시각화 (가장 중요!)
```python
# 1. RGB 이미지 로드 (원본 해상도)
rgb_path = base_dir / "image_a6" / f"{stem}.jpg"
rgb_image_original = cv2.imread(str(rgb_path))  # 원본 해상도 RGB

# 2. RGB를 리사이징된 해상도로 리사이즈
rgb_image_resized = cv2.resize(
    rgb_image_original, resized_image_size,  # (640, 512) 또는 (640, 384)
    interpolation=cv2.INTER_AREA  # 보간법: 선형 보간
)

# 3. 깊이맵도 리사이징된 해상도 (이미 프로젝션으로 생성함)
depth_map_resized  # 이미 (640, 512) 또는 (640, 384) 크기

# 4. RGB 위에 깊이맵 포인트 오버레이
point_size = 4 if resized_image_size[0] >= 1280 else 2  # 해상도에 따라 다른 크기
rgb_with_depth = create_rgb_with_depth_scatter(
    rgb_image_resized.copy(),
    depth_map_resized,
    point_size=point_size,  # 640×512: 2, 640×384: 2
    max_depth=15.0
)
cv2.imwrite(str(resized_viz_path), rgb_with_depth)
```

**시각화 결과**:
```
640x512_newest/newest_viz_results/0000000001_depth_analysis.png
└─ RGB 이미지 (640×512) + JET colormap 포인트 오버레이

640x384_newest/newest_viz_results/0000000001_depth_analysis.png
└─ RGB 이미지 (640×384) + JET colormap 포인트 오버레이
```

#### E. 다른 출력물들
```python
# Colormap 이미지
create_depth_colormap_image(depth_map_resized, resized_colormap_path)

# Diff 이미지 (merged, synth, orig)
save_diff_depth_colormap(resized_diff_merged_path, depth_merge_resized, ...)
save_diff_depth_colormap(resized_diff_synth_path, depth_synth_resized, ...)
save_diff_depth_colormap(resized_diff_orig_path, depth_orig_resized, ...)

# 로그 출력
print(f"[{res_name}] Saved all {res_name} outputs for {stem}")
# ➜ [640x512] Saved all 640x512 outputs for 0000000001
# ➜ [640x384] Saved all 640x384 outputs for 0000000001
```

---

### 📤 4단계: 최종 요약 (라인 1407-1413)

```python
# 기존 (하드코딩)
if process_resized:
    print(f"[640x384] All 640x384 outputs saved to: {resized_base_dir}")

# 수정됨 (동적)
for res_size in resized_resolutions:
    res_name = f"{res_size[0]}x{res_size[1]}"
    if process_resized[res_size]:
        print(f"[{res_name}] All {res_name} outputs saved to: {resized_dirs[res_size]['base']}")
        # ➜ [640x512] All 640x512 outputs saved to: /path/to/640x512_newest
        # ➜ [640x384] All 640x384 outputs saved to: /path/to/640x384_newest
```

---

## 📋 전체 처리 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│ 프로그램 시작                                                     │
│ python integrated_pcd_depth_pipeline_newest.py                   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [INIT] 라인 1058-1103: 초기화                                    │
│ ┌──────────────────────────────────────────────────────────────┐
│ │ resized_resolutions = [(640, 512), (640, 384)]               │
│ │                                                              │
│ │ for res_size in [(640, 512), (640, 384)]:                   │
│ │   ├─ 640x512_newest/ 폴더 구조 생성 (없으면)                │
│ │   │  └─ newest_pcd/, newest_depth_maps/, 등등              │
│ │   └─ 640x384_newest/ 폴더 구조 생성 (없으면)                │
│ │      └─ newest_pcd/, newest_depth_maps/, 등등              │
│ │                                                              │
│ │ process_resized = {(640,512): True, (640,384): True}        │
│ │ resized_dirs = {(640,512): {...}, (640,384): {...}}         │
│ └──────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [MAIN LOOP] PCD 파일들 반복 처리                                  │
│                                                                  │
│ for pcd_file in pcd_files:  # 0001.pcd, 0002.pcd, ...         │
│   ├─ [1920×1536] 원본 해상도 처리                              │
│   │  ├─ 깊이맵 생성                                            │
│   │  ├─ RGB+depth 시각화                                       │
│   │  └─ Colormap, diff 저장                                    │
│   │                                                             │
│   └─ [다중 해상도] 라인 1219-1298                              │
│      │                                                         │
│      ├─ for resized_image_size in [(640, 512), (640, 384)]:   │
│      │  │                                                     │
│      │  ├─ [640×512] 처리                                    │
│      │  │  ├─ 깊이맵 생성 (독립적 프로젝션)                   │
│      │  │  ├─ RGB 리사이즈 (1920×1536 → 640×512)            │
│      │  │  ├─ RGB+depth 시각화                               │
│      │  │  ├─ Colormap 생성                                  │
│      │  │  ├─ Diff 이미지 생성                               │
│      │  │  └─ 저장: 640x512_newest/newest_viz_results/       │
│      │  │     └─ 0001_depth_analysis.png                    │
│      │  │                                                     │
│      │  └─ [640×384] 처리 (위와 동일)                        │
│      │     └─ 저장: 640x384_newest/newest_viz_results/       │
│      │        └─ 0001_depth_analysis.png                    │
│      │                                                         │
│      └─ 다음 PCD 파일...                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ [SUMMARY] 라인 1407-1413: 최종 요약                             │
│ ┌──────────────────────────────────────────────────────────────┐
│ │ Output directories:                                         │
│ │   - newest_pcd: /path/to/newest_pcd                         │
│ │   - newest_depth_maps: /path/to/newest_depth_maps           │
│ │   - ...                                                     │
│ │                                                              │
│ │ [640x512] All 640x512 outputs saved to: .../640x512_newest  │
│ │ [640x384] All 640x384 outputs saved to: .../640x384_newest  │
│ └──────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 640×512 추가 해석

### "왜 따로 처리해야 하나?"

```
1920×1536 원본 이미지로 이미 깊이맵 생성했는데,
왜 640×512는 별도로 또 생성?

이유 1: 정확도
  ❌ 1920×1536 깊이맵 → 보간으로 축소 → 정확도 손실
  ✅ 1920×1536에서 카메라 프로젝션 → 640×512 좌표 → 정확한 깊이

이유 2: 포인트 밀도
  640×512는 1920×1536의 ~13% 면적
  보간 시: 포인트 누락 가능
  프로젝션 시: 정확한 포인트 배치

이유 3: 응용
  - 실시간 처리 (저해상도 빠름)
  - 모바일 / 경량 디바이스
  - 다양한 카메라 해상도 지원
```

### "폴더가 별도로 생성되는 이유?"

```
640x512_newest/
  ├─ newest_pcd/            ← 640×512용 PCD
  ├─ newest_depth_maps/     ← 640×512용 깊이맵 (PNG 16bit)
  ├─ newest_viz_results/    ← 640×512용 RGB+depth 시각화
  ├─ newest_colormap/       ← 640×512용 컬러맵
  ├─ newest_synthetic_depth_maps/
  └─ diff_results/

이유:
- 해상도별 출력물 구분
- 원본 1920×1536과 혼동 방지
- 재실행 시 중복 확인 용이
- 대역폭 절약 (각 해상도 독립 관리)
```

---

## 🔧 커스터마이즈

### 1️⃣ 다른 해상도 추가

```python
# 라인 1061
resized_resolutions = [
    (640, 512),    # 기존
    (640, 384),    # 기존
    (1280, 960),   # 추가 ← 이것만 추가!
    (512, 384),    # 추가 ← 이것만 추가!
]

# 나머지 코드는 그대로!
# 자동으로 1280x960_newest/, 512x384_newest/ 폴더 생성
# 모든 처리 자동 적용
```

### 2️⃣ 포인트 크기 조정

```python
# 라인 1279
# 기존: 1280px 기준
point_size = 4 if resized_image_size[0] >= 1280 else 2

# 변경: 800px 기준으로 변경하고 싶으면
point_size = 3 if resized_image_size[0] >= 800 else 2
point_size = 5 if resized_image_size[0] >= 1280 else 3
```

### 3️⃣ 특정 해상도만 처리

```python
# 640x512만 처리하고 싶으면
resized_resolutions = [(640, 512)]  # 640x384 제거

# 640x384만 처리하고 싶으면
resized_resolutions = [(640, 384)]  # 640x512 제거
```

---

## ✅ 체크리스트

### 첫 실행 (새 폴더 생성)
```
python integrated_pcd_depth_pipeline_newest.py \
    --parent_folder D:\data\ncdb-cls\ncdb-cls\2025-07-11_15-00-27_410410_A\synced_data

✅ 640x512_newest/ 폴더 생성됨
✅ 640x384_newest/ 폴더 생성됨
✅ 각 폴더에 newest_viz_results/ 생성됨
✅ RGB+depth 이미지 생성됨
✅ print 메시지:
   [640x512] Saved all 640x512 outputs for 0000000001
   [640x384] Saved all 640x384 outputs for 0000000001
```

### 재실행 (폴더 이미 있음)
```
python integrated_pcd_depth_pipeline_newest.py \
    --parent_folder D:\data\ncdb-cls\ncdb-cls\2025-07-11_15-00-27_410410_A\synced_data

✅ process_resized[(640,512)] = False (skip)
✅ process_resized[(640,384)] = False (skip)
✅ 재생성 안 함 (폴더 이미 있음)
✅ 빠른 실행
```

### 폴더 재생성
```bash
# 640x512_newest 삭제 후 다시 실행
rmdir /s "640x512_newest"
python integrated_pcd_depth_pipeline_newest.py \
    --parent_folder D:\data\ncdb-cls\ncdb-cls\2025-07-11_15-00-27_410410_A\synced_data

✅ 640x512_newest/ 재생성
✅ 모든 파일 다시 생성
```

---

## 🎯 정리

| 항목 | 설명 |
|------|------|
| **실행 명령** | `python integrated_pcd_depth_pipeline_newest.py --parent_folder ...` |
| **추가 작업** | 없음 (자동) |
| **생성 폴더** | 640x512_newest/, 640x384_newest/ (자동) |
| **처리 방식** | for 루프 (모든 해상도 자동 처리) |
| **새 해상도** | resized_resolutions에 추가만 하면 됨 |
| **하드코딩** | 없음 (제거됨) |
| **확장성** | 높음 (동적 시스템) |

**이제 640×512가 완벽하게 지원됩니다!** 🎉
